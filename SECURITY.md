# 安全问题报告

请通过仓库 **Security → Report a vulnerability** 私下报告：

https://github.com/dming2016/delta-stats-book/security/advisories/new

如果入口暂不可用，请只创建一个不包含敏感内容的 Issue，说明“需要私下联系报告安全问题”。不要在公开 Issue、PR、截图或附件中粘贴凭证及可用于攻击他人的细节。

## 建议提供

- 程序版本、Windows/微信版本、运行方式。
- 受影响组件和最小复现条件。
- 可能影响的数据或权限范围。
- 已脱敏的错误片段或使用虚构数据的复现。

不要提供真实 Cookie、OpenID、token、账号保险箱、微信存储、HAR 或完整缓存。不要对他人账号或服务开展未经授权的测试。

## 支持范围

优先处理 `main` 和最新发布版本的问题；旧版本请先核实最新版是否仍受影响。项目由个人维护，目前不承诺固定响应时限。

## 已知边界

- DPAPI 保护的是当前 Windows 用户的本地凭据，不抵御已经控制该 Windows 用户会话的恶意程序。
- 本地 API 只应监听回环地址，不可直接暴露公网。
- 可选共享服务依赖独立部署者正确配置身份验证和反向代理。不要直接对外开放其监听端口。
- 更新包采用 HTTPS、大小和 SHA-256 校验；当前不是独立签名验证体系。下载站或更新清单被攻破仍可能影响更新信任。
- 官方小程序使用的接口可能变化，程序不能保证账号风险为零。
