# 贡献指南

欢迎参与三角洲战绩本。小而明确的修复比大范围无关改写更容易审查。

## 提交方式

1. 从 `main` 创建分支；较大功能先开 Issue 讨论范围。
2. 使用 Windows、Python 3.12 和独立虚拟环境。
3. 修改代码，并为修复补上能复现原问题的测试。
4. 运行 `python -m unittest -v`；界面变化附隔离演示截图。
5. 提交 PR，说明动机、实际变化、测试结果和未验证项目。

开发环境与浏览器测试见 [开发指南](docs/operator-runbook.md)。

## 必须保持的约定

- 烽火地带与全面战场保持独立模型，不混算收益、助攻或救援字段。
- 腾讯明确拒绝认证时才持久化为 expired；繁忙、网络与未知错误不能使已有登录态失效。
- 账号缓存、凭据与偏好必须隔离，不自动启用上传。
- 更新协议、安装身份和数据路径属于兼容契约，不在普通功能 PR 中顺便改名。
- 未知数据保留未知，不把缺失值改成零或编造官方没有返回的字段。
- 不引入游戏进程注入、自动操作、绕过反作弊、账号抓取或未经授权的数据采集。

## 数据与截图

测试用虚构数据。不要提交微信目录、DPAPI 保险箱、OpenID、Cookie、token、HAR、原始接口响应或生产密钥。日志请只摘取必要错误并脱敏；截图使用 `tools/preview_ui.py`，不要上传他人的游戏昵称或记录。

第三方资源需要附来源与许可证。地图占位图通过脚本生成，不接受从游戏提取的美术替换而没有相应分发许可。

## 许可证与协作

提交贡献意味着你有权提供这些内容，并愿意按项目 MIT 许可证分发；第三方内容保留原许可。请聚焦具体事实和问题，尊重不同经验的参与者，不发布个人隐私或骚扰性内容。
